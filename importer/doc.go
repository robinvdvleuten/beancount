// Package importer is the protocol between beancount and an Importer: the
// SDK an Importer is written with, and the host side that runs one. Both
// sides speak ast values; the protobuf messages stay internal.
package importer
